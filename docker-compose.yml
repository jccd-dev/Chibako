services:
  chibako:
    build: .
    container_name: chibako
    restart: unless-stopped
    environment:
      - DATA_DIR=/app/data
      - SITE_NAME=${SITE_NAME:-Chibako}
    volumes:
      - ./data:/app/data
    networks:
      - chibako
    expose:
      - "3000"

networks:
  chibako: